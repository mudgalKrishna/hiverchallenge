# ---------------------------------------------------------------------------
# CloudSync AI Suggested-Response System -- requirements.txt
# Exhaustive dependency list intended to run cleanly on a fresh Google Colab
# instance or a local CLI environment with zero ModuleNotFoundError issues.
# ---------------------------------------------------------------------------

# --- Core data handling ---
pandas>=2.0.0
numpy>=1.24.0,<2.0.0

# --- Environment / config ---
python-dotenv>=1.0.0
pydantic>=2.0.0

# --- HTTP / API calls ---
requests>=2.31.0
httpx>=0.27.0
urllib3>=2.0.0

# --- LLM provider SDKs ---
openai>=1.30.0
huggingface_hub>=0.23.0

# --- RAG / embeddings ---
sentence-transformers>=2.7.0
transformers>=4.41.0
torch>=2.2.0
scikit-learn>=1.4.0
scipy>=1.11.0

# --- Tokenization support (used internally by transformers/sentence-transformers) ---
tokenizers>=0.19.0
sentencepiece>=0.2.0
accelerate>=0.30.0

# --- Progress bars / utilities used by HF + sentence-transformers ---
tqdm>=4.66.0
filelock>=3.13.0
packaging>=23.2
regex>=2023.12.25

# --- Optional but commonly needed alongside the above on Colab/CLI ---
einops>=0.7.0
safetensors>=0.4.0
protobuf>=4.25.0
PyYAML>=6.0.0

# --- Data validation / structured output parsing ---
jsonschema>=4.22.0

# --- Plotting / quick result inspection (optional, harmless to include) ---
matplotlib>=3.8.0

# --- Testing (optional, useful for CI on the GitHub repo) ---
pytest>=8.0.0
